# BunnyTail.Resolver — Source Generator による AOT セーフ DI ライブラリ 仕様ドラフト

Status: Draft v0.5 (2026-08-15) — 名称確定(BunnyTail.Resolver)。検証ベンチマーク完了(VB-01〜06 + VB-01b、JIT + NativeAOT、判定は [__benchmarks/results/VERDICT.md](__benchmarks/results/VERDICT.md))。M1 完了(互換テスト 143/143)。対象は .NET 10 専用(ジェネレータ本体のみ netstandard2.0)

## 1. 概要

Source Generator によりコンパイル時に依存解決コードを生成する、AOT セーフな **MEDI 互換** DI ライブラリ。目的は単一で、「MEDI(Microsoft.Extensions.DependencyInjection)として使えて、MEDI より速い」こと。動作モードの区別は持たない。

- **MEDI 完全互換**: `Microsoft.Extensions.DependencyInjection.Specification.Tests` を 100% パスする。`IServiceProviderFactory<T>` により MEDI へ差し替え可能。セマンティクス(ライフタイム・disposal・コレクション・keyed)は常に MEDI に一致させる。
- **AOT セーフ**: 解決経路に `Reflection.Emit` / `Expression.Compile` / `MakeGenericType` を使わない。NativeAOT・トリミング対応(`IsAotCompatible`)。
- **最速**: コンパイル時に確定できる処理(型解析・コンストラクタ選択・ライフタイム形状・スロット割当・循環検証)はすべて生成時に済ませ、実行時は最小の lookup と分岐のみにする。
- **登録の自動化**: 属性・MEDI 登録 API 呼び出し・命名規約(正規表現)の 3 系統から登録情報を収集し、全コンポーネントの手動登録を不要にする。

### 1.1 非ゴール

- Smart.Resolver が持つ動的な高機能(metadata constraint、MissingHandler、JSON/XML 定義ロード、実行時 `Inject(object)`)。
- Unity 対応。
- メソッド注入(プロパティ注入は `[Inject]` として採用 — 3.4 章)。
- .NET 10 未満のランタイムサポート(ランタイムは net10.0 専用。ジェネレータ本体のみ Roslyn ホスト都合で netstandard2.0)。
- スタンドアロンの静的リゾルバ(process-static な Singleton 保持)。Singleton はプロバイダインスタンスのメンバとして保持し、MEDI の「プロバイダごとの Singleton」セマンティクスのみをサポートする。

### 1.2 参考にした実装と採用する手法

| 出典 | 採用する手法 |
|---|---|
| Smart-Net-Resolver | `IServiceProviderFactory<T>` + `Populate(IServiceCollection)` による MEDI 差し替え構造。Specification Tests 継承による互換テスト。Scoped のスロット(連番+配列)解決。 |
| Generator2-ServiceRegistration | 正規表現による命名規約ベース自動登録。partial 拡張メソッドをアンカーとした登録メソッド生成。複数インタフェースのフォワーディング登録。 |
| Generator2 共通規約 (generator-guide) | `IIncrementalGenerator` パイプライン構成、`SourceGenerateHelper`、診断と生成の分離、`analyzers/dotnet/cs` パッケージング、2 層テスト。 |
| dotnet-performance (usausa) | 実測済み性能パターンカタログ。採用パターンは 6 章に列挙。生成コードの形は `docs/generated-code-patterns.md` シナリオ 4(Factory/DI 解決)に準拠。ベンチマーク作法は `docs/benchmark-methodology.md` に準拠。 |

## 2. アーキテクチャ

### 2.1 生成されるプロバイダ(インスタンスメンバ方式)

ジェネレータは partial クラスをアンカーに、`IServiceProvider` 一族を実装するプロバイダクラスを生成する。**Singleton はプロバイダインスタンスの型付きフィールド**、Scoped はスコープインスタンスのスロット配列として保持する。static メンバによる状態保持は行わない(複数プロバイダの共存・破棄・テスト分離という MEDI セマンティクスのため)。

```csharp
[GenerateServiceProvider]
public sealed partial class AppServiceProvider;
```

生成イメージ(概形):

```csharp
// <auto-generated />
public sealed partial class AppServiceProvider :
    IServiceProvider, IKeyedServiceProvider, IServiceScopeFactory,
    IServiceProviderIsService, IServiceProviderIsKeyedService,
    IDisposable, IAsyncDisposable
{
    // Singleton: 型付きインスタンスフィールド(lazy、MEDI セマンティクス)
    private Component1? singleton0;

    internal Component1 GetComponent1()
        => singleton0 ?? InitComponent1();          // 生成時に disposal 追跡要否も確定

    internal Component2 GetComponent2()
        => new(GetComponent1());                    // 依存はリテラル展開(delegate 間接呼び出しなし)

    public object? GetService(Type serviceType) { /* 4.1 の dispatch */ }
}
```

- 依存グラフは生成時に解決済みなので、生成メソッド間の直接呼び出しになる(`Func` の連鎖を作らない — generated-code-patterns シナリオ 4 / GEN-01 実測: 子ファクトリ連鎖は 1.7 倍遅い)。
- Singleton の lazy 初期化はダブルチェックで生成。`ValidateOnBuild` 相当の検証はコンパイル時診断が兼ねる。
- スコープは生成される `Scope` 型(プロバイダの nested class)。Scoped スロットのインデックスは生成時に確定した定数。

### 2.2 解決の 2 経路

| 経路 | 対象 | 実装 |
|---|---|---|
| **生成経路** | コンパイル時に見える登録(属性・ユーザーコード内の `Add*` 呼び出し・命名規約) | 生成された `new` 直書きメソッド。リフレクションゼロ。 |
| **互換経路** | 実行時にのみ判明する `ServiceDescriptor`(コンパイル済みアセンブリからの登録、`ImplementationFactory` / `ImplementationInstance`) | `ConstructorInfo.Invoke` ベース(Emit 不使用)。メタデータが保持されていれば NativeAOT でも動作。 |

この分離が必須である理由: MEDI 公式 Specification Tests は**コンパイル済みアセンブリ**が実行時に `ServiceCollection` を構築するため、ジェネレータからは登録が見えない。互換テスト 100% は互換経路が担い、生成経路は同一セマンティクスの高速化レイヤーとして機能する。

両経路のセマンティクス一致は、ライフタイム管理・disposal 追跡・コレクション解決を共通ランタイムに集約し、経路差分を「インスタンス生成関数の中身」だけに限定して担保する。

### 2.3 結線とビルド時検証

`BuildServiceProvider` / `IServiceProviderFactory.CreateServiceProvider` 時に、実行時の `IServiceCollection` と生成時の前提を突き合わせる:

1. 各 `ServiceDescriptor` について、(ServiceType, ImplementationType, Lifetime, Key) が生成済みエントリと一致すれば**生成経路**のアクセサを、不一致(Replace/TryAdd による差し替え、ファクトリ/インスタンス登録、未知の型)なら**互換経路**のアクセサをサービステーブルに配置する。
2. 生成ファクトリが前提とするコンストラクタ選択(MEDI 規則)が実行時の登録集合でも成立するかを検証し、不成立なら互換経路へフォールバックする。「速いが挙動が違う」事故を構造的に防ぐ。
3. テーブルは以後イミュータブル。resolve 経路に同期は存在しない(書き込みはビルド時のみ。動的追加が必要な内部キャッシュは COW 配列 + `Volatile.Read` — TypeMap の並行形状)。

結線方式は `[ModuleInitializer]` による実装型→生成アクセサのレジストリ登録(型名文字列による実行時 lookup を伴わないため初回コストが小さい)。アンカーのプロバイダクラスを使う場合はレジストリを経由せず直接結線する。

### 2.4 プロジェクト構成

```
BunnyTail.Resolver/                                  ランタイム+属性 (net10.0 専用, IsAotCompatible)
BunnyTail.Resolver.Generator/                        ソースジェネレータ (netstandard2.0, Roslyn 最新)
BunnyTail.Resolver.Extensions.DependencyInjection/   MEDI ブリッジ (IServiceProviderFactory 等)
BunnyTail.Resolver.Tests/                            ジェネレータ harness + 機能テスト
BunnyTail.Resolver.CompatibilityTest/                MEDI Specification Tests
BunnyTail.Resolver.AotTests/                         PublishAot 検証
BunnyTail.Resolver.Benchmark/                        検証ベンチ (11 章) + 対外比較ベンチ
Develop/ Develop.Library/                            ジェネレータデバッグ用 (DebugRoslynComponent)
```

パッケージングは Generator2 定型: Generator DLL と `SourceGenerateHelper.dll` を `analyzers/dotnet/cs` へ、`CompilerVisibleProperty` は `build/*.props` へ同梱。

## 3. 登録モデル(3 系統の収集)

### 3.1 属性ベース

```csharp
[Singleton]                                   // 自己登録 + 実装インタフェースへ登録
public class Component1;

[Transient]
public class Component2(Component1 component);

[Scoped(As = typeof(IService))]               // 登録先サービス型の明示
public class Component3 : IService, IOther;

[Singleton(Key = "primary")]                  // keyed 登録
public class Component4 : IService;
```

- `[Singleton]` / `[Scoped]` / `[Transient]`。`AllowMultiple = true`。
- `As`(省略時は実装インタフェース全て+自己型。`IDisposable` / `IAsyncDisposable` は除外)、`Key`(keyed services)。
- 複数サービス型への Singleton/Scoped 登録はフォワーディングで同一インスタンスを保証。

### 3.2 MEDI 登録 API の収集

ユーザーコード内の `AddSingleton<T>` / `AddScoped<T,TImpl>` / `AddTransient` / `TryAdd*` / `AddKeyed*` 呼び出し箇所を走査し、型引数・`typeof` 引数から生成対象を収集する。既存の MEDI 登録コードを一切変更せずに生成経路の恩恵を受ける。

### 3.3 命名規約ベースの自動登録(登録メソッド生成)

```csharp
public static partial class ServiceCollectionExtensions
{
    [ComponentRegistration(Lifetime.Singleton, "Service$")]
    [ComponentRegistration(Lifetime.Scoped, "Repository$", Namespace = "MyApp.Data")]
    public static partial IServiceCollection AddComponents(this IServiceCollection services);
}
```

- Generator2-ServiceRegistration の仕様を踏襲(`[StringSyntax(Regex)]`、`Namespace` / `Assembly` フィルタ、参照アセンブリ走査のオプトイン、除外インタフェース設定)。
- 生成される本体は `services.AddSingleton<IFoo, Foo>()` の列であり、3.2 の収集対象にもなるため自動的に生成経路に乗る。

### 3.4 [Inject] 属性(プロパティインジェクション)

```csharp
[Transient]
public class Component
{
    public Component(Dependency1 dep1) { ... }   // コンストラクタ注入 (通常)

    [Inject]
    public Dependency2 Dep2 { get; set; } = default!;   // インスタンス生成後にプロパティ注入

    [Inject]
    [FromKeyedServices("primary")]
    public Dependency3 Dep3 { get; set; } = default!;   // keyed 指定も可
}
```

- `[Inject]` を付与した public settable プロパティに対し、**インスタンス生成後にプロパティ注入**を行う。生成コードでは `var x = new Component(...); x.Dep2 = ...;` の形にインライン展開される。
- MEDI に存在しない拡張機能だが、両経路のセマンティクス一致原則に従い**互換経路(リフレクション)でも同じ動作**をする(コンテナがインスタンス化する型すべてが対象。`ImplementationInstance` / `ImplementationFactory` 登録は対象外 — 生成をコンテナが行わないため)。
- 解決不能な `[Inject]` プロパティはエラー(コンパイル時診断 + 実行時 InvalidOperationException)。
- `[FromKeyedServices]` の併用可(キー継承・明示 null の規則はコンストラクタ引数と同一)。メソッド注入は非対応。

## 4. 解決経路の設計(lookup)

### 4.1 非ジェネリック経路: `GetService(Type)`

MEDI の主要エントリポイント。ASP.NET Core / Generic Host のフレームワーク内部呼び出しはすべてこの非ジェネリック署名を通るため、**性能上の本丸はここ**。用途は「`Type` → 解決処理(サービステーブルのエントリ/生成アクセサ)」のルックアップである。

**方式確定(VB-01 + VB-01b 実測)**: **Fixed ノードハッシュテーブル**(容量 2^n + `RuntimeHelpers.GetHashCode(type)` + ビットマスク、**バケットは Node 直置き + Next 連結 + EmptyNode センチネル**(Smart-Net `ThreadsafeTypeHashArrayMap` と同じレイアウトのイミュータブル版)、構築後イミュータブル)を採用する。VB-01b の 2×2 分離実験で、ノードリストレイアウトは Entry[] 小配列バケットに Hit 8〜19% / Miss 12〜21% で有意勝利(バケット先頭ヒット時の indirection が1段少ない)、identity hash は仮想 `type.GetHashCode()` に Hit 15〜29% / Miss 34〜51% で有意勝利(逆アセンブル確認: identity はインライン化、virtual は PGO の部分デバーチャライズ後も仮想呼び出しが残り GDV 依存の変動あり)。

| 候補 | Hit 実測 JIT (N=8〜512) | Hit 実測 AOT | 判定 |
|---|---|---|---|
| `Dictionary<Type, Entry>`(基準) | 4.29〜4.88 ns | 10.8〜11.4 ns(2.3 倍劣化) | 基準 |
| **Fixed(採用)** | **1.74〜2.21 ns(0.40〜0.45x)** | **2.66〜2.89 ns(0.24〜0.26x)** | 全 N・Hit/Miss・両ランタイムで有意勝利。AOT で優位がさらに拡大 |
| Robin Hood(PSL + 8bit fingerprint パック、SoA 分離) | 1.99〜2.61 ns | 3.08〜3.30 ns | 次点。常に Fixed より有意に遅い |
| Type→slot→配列の二段 | 4.93〜5.87 ns(1.12〜1.21x) | 11.1〜11.6 ns | 不採用(TYP-01 の結論を追認) |
| `FrozenDictionary<Type, Entry>`(対照) | 6.03〜6.82 ns(1.40x) | 10.0〜10.3 ns | 不採用確定(R-08 追認) |

**テーブルエントリの dispatch 形状(VB-03 実測で確定)**: エントリは**生成プロバイダのインスタンスメソッドを指す closed delegate(`Func`)**とする。エントリごとに別メソッドを指す現実的条件で、AOT 2.31 ns(最速・有意)/ JIT 1.6〜1.9 ns。sealed activator クラスの仮想呼び出しは JIT で bimodal(1.18〜2.57 ns、PGO ガード投機の当否が起動単位で変動)かつ AOT で 2.44 ns の次点。interface dispatch(AOT 3.24)・static delegate(3.56)・struct+delegate(3.58)・`delegate*`(3.07、calli のインライン不可は AOT でも同じ)は不採用。

### 4.2 Slot<T> 連番方式の検証

「ソース生成なのだからハッシュではなく `Slot<T>` に連番を振り、インデックスで解決できないか」— この手法は dotnet-performance に **TYP-01(TypeMap / TypeSlot)** として存在し、実測済み:

```csharp
public static class TypeSlot { private static int nextIndex = -1;
    public static int Allocate() => Interlocked.Increment(ref nextIndex); }
public static class TypeSlot<T> { public static readonly int Index = TypeSlot.Allocate(); }
```

| 経路 | 実測 (8 型) | 対 Dictionary |
|---|---:|---|
| ジェネリック `TryGetValue<T>()`(slot が JIT 定数 → 素の配列アクセス) | **0.226 ns** | **約 11 倍速** |
| 非ジェネリック `Type` → slot → 配列(二段 lookup) | 10.41 ns | **4.2 倍遅い** |

**結論(VB-01/VB-02 で実測確認済み)**: Slot 方式が勝つのは「インデックスがコンパイル時/JIT 定数になる経路」だけである。非ジェネリック `GetService(Type)` では Type→slot の対応にどのみち辞書が必要で、二段 hop の分だけ直接 `Type→Entry` より遅い(実測 1.12〜1.21x)。したがって:

- **非ジェネリック経路**: `Type → Entry` の直接テーブル(4.1 の Fixed)。Slot は使わない。
- **生成コード内部**: 依存参照は生成時に確定するので lookup 自体が存在しない(メソッド直接呼び出し)。Scoped のスロットインデックスは**ジェネレータが振る連番定数**であり、`TypeSlot<T>` の実行時採番すら不要。
- **ジェネリック公開 API(4.3)**: `typeof(T)` 分岐チェーンを採用(VB-02 実測: JIT 0.23 ns / AOT 0.26 ns、**128 分岐でも JIT・ILC 両方で定数畳み込みが維持**され、未登録型の miss 経路は 9B のコードに消滅)。`TypeSlot<T>` + 配列(JIT 0.46 / AOT 0.82 ns)は実行時に追加された登録のフォールバックとして使う。

なお `TypeSlot<T>` の実行時採番はプロセス全域で共有されるため、複数プロバイダではスロット配列が疎になる。採用する場合も generic static 初期化子は**絶対に例外を投げない**形にする(`TypeInitializationException` は永続キャッシュされる — TYP-04/README 1481 の警告)。

### 4.3 ジェネリック/型付き高速経路

MEDI の `GetService<T>(this IServiceProvider)` 拡張は内部で `GetService(typeof(T))` を呼ぶため、標準経路の性能は 4.1 で決まる。それに加えて:

- プロバイダ自身に `T? GetService<T>()` を生成し、直接利用者にはハッシュ lookup なしの経路を提供する。実装は `typeof(T) == typeof(X)` 分岐チェーン(VB-02 実測で 128 分岐まで JIT/AOT とも約 0.25 ns、Dictionary 比 18〜40 倍速を確認)。
- 生成された型付きアクセサ(`GetComponent1()` 等)は lookup ゼロの最速経路。
- C# interceptors による `GetService<T>()` 呼び出し箇所の静的解決置換は将来オプション(13 章)。

## 5. ランタイム設計

### 5.1 ライフタイム

| ライフタイム | 実装(VB-04 実測で確定) |
|---|---|
| Transient | 生成メソッド直接呼び出し。`IDisposable` / `IAsyncDisposable` 実装型のみ disposal リストへ追加(**MEDI 互換: transient も追跡**)。追跡要否は生成時に型で確定する(VB-06 実測: 実行時 `is` チェックは AOT で +41%(非 disposable)/+16%(disposable)、JIT でもコードサイズ 7 倍)。 |
| Scoped | **生成スコープの型付きフィールド**(VB-04 実測: スコープ生成+初回解決 6.94 ns/48B vs `object[]` 方式 12.44 ns/136B。読み出しは同等)。実行時にのみ判明した Scoped 登録は `object[]` オーバーフロー(連番スロット、読み出しは `Unsafe.As` — castclass は実測で唯一有意に遅い 0.85 vs 0.65 ns)。 |
| Singleton | プロバイダインスタンスの型付きフィールド + ダブルチェック lazy(VB-04 実測: hot 読み出しは eager と CI 重複=同速。**MEDI の lazy セマンティクスを速度ペナルティなしで実現**)。ルートスコープでの Scoped 解決はルートに定着(MEDI 互換)。 |

### 5.2 スコープと disposal

- `IServiceScope` / `IServiceScopeFactory` / `AsyncServiceScope` 対応。
- スコープ内で注入される `IServiceProvider` は**そのスコープ自身**(Smart.Resolver の既知の非互換を解消)。`IKeyedServiceProvider` も同一インスタンス。
- disposal は生成の**逆順(LIFO)**。`IAsyncDisposable` 対応: `DisposeAsync` 経路、同期 `Dispose` で `IAsyncDisposable` のみ実装のサービスがあれば例外(MEDI 互換)。
- ルートプロバイダ dispose で Singleton を逆順 dispose。dispose 済みフラグは `Interlocked`(CON-01: lock 比 2.2 倍速・コードサイズ 1/50)。

### 5.3 コレクション・open generics・keyed

- `IEnumerable<T>` は**登録順**の `T[]` 実体を返す。単独登録もコレクション解決可能。
- open generics: 生成時に `Add*` 呼び出し・属性・依存グラフから到達可能な**閉じた型を全て収集して生成**。実行時にのみ判明する閉型は互換経路で `MakeGenericType`(`[RequiresDynamicCode]` 明示、AOTP-04: 値型引数は NativeAOT で実行時失敗があるため診断で誘導)。型制約を満たさない登録はスキップ(MEDI 互換)。
- keyed services: `[FromKeyedServices]`、`[ServiceKey]` 注入、`KeyedService.AnyKey`(登録・クエリ両方向)、null キーの非 keyed フォールバック、`GetKeyedServices<T>`。keyed テーブルは **`(typeHash * 397) ^ keyHash` の複合ハッシュ 1 テーブル**(VB-05 実測: Hit 0.81x / Miss 0.70x で Dictionary に有意勝利。Type→key の二段テーブルは 2.0x で不採用)。
- `IServiceProviderIsService` / `IServiceProviderIsKeyedService` 実装。
- `ServiceProviderOptions`(`ValidateScopes` / `ValidateOnBuild`)を実行時サポート。同等の検証をコンパイル時診断(7 章)でも提供。

### 5.4 コンストラクタ選択

MEDI 規則に準拠: 解決可能な引数のみで構成されるコンストラクタのうち最大のもの。曖昧なら例外。既定値付き引数フォールバック可。`[ActivatorUtilitiesConstructor]` を尊重。生成時に決定できる曖昧さは診断で事前検出。

## 6. dotnet-performance からの採用パターン

採用(いずれも実測済み。パターン ID は dotnet-performance README のもの):

| パターン | 適用箇所 |
|---|---|
| TYP-01 TypeMap/TypeSlot | 4.2 の分析どおり、定数インデックス化できる経路のみ。COW 配列 + `Volatile.Read` + 符号なし境界チェック 1 回の並行形状は内部キャッシュ全般に採用 |
| TYP-05 `Unsafe.As` | ランタイム内部で不変条件が保証されたキャスト(0.63 倍・コードサイズ 274B→33B)。private 境界内に限定し、Debug ビルドは通常キャスト + `Debug.Assert` |
| TYP-04/06 generic static キャッシュ | 採用時は初期化子を絶対に投げない形にする(例外の永続キャッシュ回避) |
| JIT-03 `typeof(T)` 分岐畳み込み | ジェネリック API 内の型特殊化 |
| JIT-04 cold path 分離 | throw・初期化・grow を `[NoInlining]` ヘルパへ分離し、hot な resolve メソッドをインライン可能に保つ |
| JIT-05 `IsReferenceOrContainsReferences<T>` | バッファクリア等の型分岐畳み込み |
| DSP-01 sealed 具象型 | 生成クラス・ランタイムクラスは既定で `sealed`(AOT では guarded devirt がないため特に効く) |
| GEN-01 emit 形状 | 子ファクトリの `Func` 連鎖禁止(1.7 倍遅い)。依存はリテラル展開 |
| CON-01 `Interlocked` | dispose フラグ等の状態遷移 |
| STK-07 | lazy/eager 初期化の使い分け判断基準 |
| generated-code-patterns シナリオ 4 | 生成方針の全体形(依存グラフのリテラル展開、実行時 `Type` 解決のみテーブル) |
| benchmark-methodology | 11 章のベンチマーク作法(等価性検証、アクセス順パラメータ化、CI 重複による 3 軸ノイズ判定) |

不採用(実測により棄却済み、または禁止リスト):

- `FrozenDictionary`(R-08 + VB-01 実測: `Type` キーで Dictionary より 1.4 倍遅い)
- **`delegate*`(関数ポインタ)ファクトリテーブル**(VB-03 実測: JIT で closed instance delegate より遅く、**AOT でも 3.07 vs 2.31 ns で劣位**。`calli` のインライン不可は両ランタイム共通)
- `Func<object>.Invoke` への `call` 強制(R-17: codegen 同一)
- `static readonly typeof(X)` キャッシュ(R-01)、手書きハッシュループ、実行時 `Type` キー辞書を主経路にすること(禁止リスト 9 節)

## 7. 診断

ID プレフィックス: **`BTRS`(確定)**。Severity 方針は Generator2 規約(生成不能→Error、生成は続くが意図と異なる→Warning)。

| ID | 内容 | Severity | 状態 |
|---|---|---|---|
| BTRS0001 | 登録メソッド宣言の形式不正(static partial 拡張メソッド + IServiceCollection) | Error | **実装済み** |
| BTRS0002 | 正規表現パターン不正 | Warning | **実装済み** |
| BTRS0003 | 循環依存(コンパイル時 DFS、パス付きメッセージ) | Error | **実装済み** |
| BTRS0004 | 未解決の依存(コンパイル対象アセンブリ内の型で、コンパイル時に見える登録に無いもののみ。実行時登録は見えないため Warning に留める) | Warning | **実装済み** |
| BTRS0005 | captive dependency(Singleton → Scoped 依存。ValidateScopes のコンパイル時版) | Warning | **実装済み** |
| BTRS0006 | コンストラクタ選択の曖昧さ(同数の最大 public ctor が複数、非 superset) | Error | **実装済み** |
| BTRS0007 | AOT 非互換の登録(実行時 open generic + 値型引数等) | Warning | M5 |

## 8. ジェネレータ実装規約

Generator2 エコシステム規約(`Smart-Net-Data-Accessor/docs/generator-guide.ja.md`)に準拠:

- `IIncrementalGenerator`。`Initialize` は配線のみ、Parser / Resolver / Diagnostics / Generator のセクション分割。
- `ForAttributeWithMetadataName` + 軽量 predicate。`Add*` 呼び出し収集は `CreateSyntaxProvider`(InvocationExpression フィルタ)。
- パイプラインには value-equatable なモデルのみ(`EquatableArray<T>` / `LocationInfo` / `Result<T>`。Symbol / SyntaxNode / Compilation / Location を持たない)。
- 診断は `RegisterSourceOutput`、生成は単位化して `RegisterImplementationSourceOutput`。
- マーカー属性はランタイム側に手書き(`RegisterPostInitializationOutput` 不使用)。
- 生成は `SourceBuilder`、`global::` 完全修飾、`// <auto-generated />` + `#nullable enable`。
- Generator TFM は `netstandard2.0`、`IsRoslynComponent=true`、`EnforceExtendedAnalyzerRules=true`。

## 9. MEDI 差し替え

```csharp
// Generic Host
builder.Host.UseServiceProviderFactory(new ResolverServiceProviderFactory());
```

- `IServiceProviderFactory<TBuilder>` 実装。`CreateBuilder` で `IServiceCollection` を取り込み、`CreateServiceProvider` で 2.3 の検証・結線を実行してプロバイダを構築。
- `IServiceScopeFactory` / `IServiceProviderIsService` / `IServiceProviderIsKeyedService` を暗黙登録。
- keyed 変換(`KeyedImplementationType/Factory/Instance`、AnyKey)を含め全 `ServiceDescriptor` 形態をサポート。

## 10. 性能目標

| 経路 | 目標 |
|---|---|
| 生成された型付きアクセサ / `GetService<T>()` | lookup ゼロ。フィールド読み+分岐 1 回(Singleton)、直接 `new`(Transient)、配列インデックス(Scoped) |
| `GetService(Type)` 生成経路 | VB-01 勝者テーブル 1 hop + 直接呼び出し。全カテゴリで MEDI 超え |
| 互換経路 | Smart.Resolver(Emit 版)と同等以上 |
| ビルド時間 | `BuildServiceProvider` はテーブル構築+検証のみ。初回 resolve のスパイクなし |

## 11. 検証用ベンチマーク(実施済み)

**実施済み(2026-08-14〜15、JIT + NativeAOT)**。実装は [__benchmarks/BunnyTail.Resolver.Benchmark](__benchmarks/BunnyTail.Resolver.Benchmark/README.md)、判定の詳細は [__benchmarks/results/VERDICT.md](__benchmarks/results/VERDICT.md)、生データは `__benchmarks/results/jit/`(JIT + 逆アセンブル)と `__benchmarks/results/jit-aot/`(VB-01/02/03/06 の JIT+NativeAOT 対比)。判定は 4〜6 章へ反映済み。

### 11.1 作法(benchmark-methodology 準拠)

- BenchmarkDotNet 0.15.x、`MediumRunJob`、`MemoryDiagnoser` + `DisassemblyDiagnoser`(maxDepth 3, exportDiff)、`MarkdownExporter.GitHub`。Mean/Min/Max/P90/Error/StdDev 列。
- 実行前に全候補の**等価性検証**(同一入力→同一結果)を必須とする(dotnet-performance の `Program.cs` 方式)。
- アクセス順をパラメータ化(forward / reverse / random / miss 混在)。最良ケースだけ測らない。
- ノイズ判定は 3 軸(速度・アロケーション・コードサイズ)。CI が重ならなければ 0.2 ns 差でも有意。CI が重なる場合、codegen が異なれば「測定ノイズ」として記録、命令列が同一なら「差なし」として棄却。
- JIT(net10.0)と **NativeAOT** の両ジョブで測定する(devirt/関数ポインタの結論が JIT と AOT で逆転しうるため)。

### 11.2 ベンチマーク項目

| ID | 検証内容 | 候補 | 判定基準 / 決まる仕様 |
|---|---|---|---|
| VB-01 | **`Type → Entry` lookup 方式**(4.1) | `Dictionary<Type,Entry>` (基準) / fixed 2^n + `RuntimeHelpers.GetHashCode` / Robin Hood (PSL+fingerprint) / `FrozenDictionary` (対照) | N = 8 / 32 / 128 / 512(実アプリの登録数帯)、hit/miss 混在。JIT + AOT。勝者が `GetService(Type)` の主テーブルになる |
| VB-02 | **Slot<T> / ジェネリック経路**(4.2, 4.3) | `TypeSlot<T>.Index` + インスタンス配列 / `typeof(T)` 分岐チェーン(N=8/32/128 で畳み込み限界を確認) / VB-01 勝者 dictionary 経由 | 当ライブラリの形状(インスタンスプロバイダ、プロバイダ複数共存)で TYP-01 の結論が再現するか。`GetService<T>()` の実装方式が決まる |
| VB-03 | **ファクトリ dispatch 形状**(2.1, 6) | 生成 sealed クラスの仮想呼び出し / インスタンスメソッドへの closed delegate / `delegate*`(対照) / エントリ struct + delegate フィールド | JIT + **AOT 必須**(DSP-02 の 5.8 倍は JIT 固有の可能性)。サービステーブルのエントリ表現が決まる |
| VB-04 | **Singleton/Scoped 保持形状**(2.1, 5.1) | Singleton: 型付きフィールド + ダブルチェック vs `object[]` スロット。Scoped: `object[]` 定数インデックス vs 生成型付きフィールド vs `Dictionary` | 生成プロバイダ/スコープのフィールドレイアウトが決まる。lazy 分岐コスト(STK-07: 1.24 倍)も計測 |
| VB-05 | **keyed 複合キー**(5.3) | `(Type, key)` 合成ハッシュ 1 テーブル vs Type テーブル → key サブテーブル 2 段 | keyed テーブル構造が決まる |
| VB-06 | **disposal 追跡コスト**(5.2) | 追跡なし型の resolve(分岐ゼロ確認)/ List append / 逆順 dispose | 生成時追跡確定の効果検証。codegen で分岐ゼロを確認 |
| VB-07 | **end-to-end 比較**(参考値・継続測定) | 本ライブラリ vs MEDI vs Smart.Resolver。Singleton / Transient / Scoped+スコープ生成 / 複雑グラフ(深さ 4〜5) / `IEnumerable` / keyed | 10 章の性能目標の判定。guard 値として継続運用 |

### 11.3 確定フロー

1. VB-01〜VB-06 を実装・実測(等価性検証込み)。
2. 結果 md を `__benchmarks/results/` に保存し、各項目の勝者を 4〜6 章へ反映。
3. 本ドキュメントの Status を Draft → Fixed に更新し、実装マイルストーン(12 章)へ進む。

## 12. テスト戦略・マイルストーン

テストは 4 層: harness(`GeneratorTestRunner` + `VerifyCompiles()`)/ 機能(自己適用 + 実行検証)/ 互換(`DependencyInjectionSpecificationTests` + `KeyedDependencyInjectionSpecificationTests` 継承、**100% パスをリリース条件**)/ AOT(`PublishAot=true`、トリミング警告ゼロ)。

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| M0 | **検証ベンチマーク(11 章)** | **完了**(VB-01〜06 + VB-01b 実施、判定を 4〜6 章へ反映) |
| M1 | ランタイムコア(テーブル・ライフタイム・スコープ・disposal)+ 互換経路 + MEDI ブリッジ | **完了**(2026-08-15: Specification Tests 143/143 = 100% パス。base + keyed 両スイート、`Supports*` 有効) |
| M2a | **生成想定コードの手書きプロトタイプ**: ジェネレータが出力する予定のコード(登録メソッド・生成ファクトリ・`[ModuleInitializer]` 結線・`[Inject]` プロパティ注入)を手書きし、エンジンの生成経路フックに結線 | **完了**(2026-08-15: 機能テスト 12 + Generic Host `UseServiceProviderFactory` 統合、互換テスト 143/143 維持) |
| M2b | **Source Generator 実装**: M2a の手書きコードと同一の出力を生成するジェネレータ(属性収集) | **完了**(2026-08-15: `BunnyTail.Resolver.Generator` 実装、出力一致検証パス、テストプロジェクトを生成コードへ切替済み、全 13 + 143 テストパス) |
| M3 | `Add*` 呼び出し収集、命名規約ベース登録メソッド生成 | **完了**(2026-08-15: `AddSingleton/AddScoped/AddTransient/TryAdd*` のジェネリック呼び出しから実装型を収集し生成ファクトリを出力(既存 MEDI コード無変更)。`[ComponentRegistration(Lifetime, pattern)]` による partial メソッド本体生成(ServiceRegistration 互換の 0/1/2+ インタフェース規則、Namespace フィルタ、規約マッチ型のファクトリ生成込み)。診断 BTRS0001/0002。テスト 20 + 互換 143 パス) |
| M4 | open generics 閉型収集、keyed 完全対応、診断一式 | **ほぼ完了**(2026-08-15: keyed 生成ファクトリ(`RegisterKeyed` + `[ServiceKey]`/`[FromKeyedServices]` 継承/明示、AnyKey 経由の要求キー伝搬まで検証)、closed generic 実装型の収集対応、診断 BTRS0003〜0006 実装。テスト 26 + 互換 143 パス。**残**: `typeof(IRepo<>)` 形式の open generic 定義登録からの依存グラフ閉型収集と BTRS0007 → M5 で対応) |
| M5 | 最適化・VB-07 継続測定・パッケージング | **一部完了**(2026-08-15: FixedNode 主テーブル差し替え(ウォームアップ+イミュータブル化)、AotTests(NativeAOT 実行 10 項目 OK・AOT/トリミング警告ゼロ)、NuGet パッケージング(analyzers 同梱)、VB-07 初回測定。**VB-07 は混合結果**: Scoped 0.29x / ScopeCreate 0.55x で MEDI に大勝、Singleton/Transient ほぼ同等、Graph/Enumerable/Keyed は劣後 — 原因と対策は VERDICT.md VB-07 章参照) |
| M5b | **性能追い込み**: ①生成ファクトリのグラフインライン化(transient 依存のリテラル `new` 展開 = 5 章の最終形、Graph 5.7x の主因)②`GetService(Type)` エントリパス短縮 ③実現済み派生エントリ(IEnumerable 等)の COW テーブル昇格 ④ **AOT での VB-07 測定**(MEDI は Emit 不可でリフレクション経路に落ちるため、本ライブラリの土俵での比較) | 全カテゴリで MEDI 超え(JIT)+ AOT 比較の記録 |

## 13. 未決事項

1. ~~名称/パッケージ ID~~: **確定 — `BunnyTail.Resolver`**(Generator2 エコシステムの生成系ブランドに整合。Smart.Resolver と棲み分け)。
2. ~~対象 TFM の下限~~: **確定 — ランタイムは net10.0 専用**(ジェネレータ本体のみ netstandard2.0)。
3. ~~lookup / dispatch / 保持形状~~: **解決済み**(VERDICT.md 参照。VB-01〜06 とも JIT + NativeAOT で確認済み)。
4. **C# interceptors**: `GetService<T>()` 呼び出し箇所の静的解決置換。v1 では見送り、導入可能な設計に保つ。
5. **プロバイダアンカー(`[GenerateServiceProvider]`)の要否**: ModuleInitializer 結線のみで足りる場合、アンカーなし(生成ファクトリ+汎用プロバイダ)の方が使い勝手が良い可能性。型付きフィールド保持との両立を M2 設計時に判断(VB-04 の結果に依存)。
6. **互換経路の open generic + 値型引数**: NativeAOT で原理的に不可能なケースの扱い(例外メッセージ + SIDI0007 診断で誘導、で足りるか)。

